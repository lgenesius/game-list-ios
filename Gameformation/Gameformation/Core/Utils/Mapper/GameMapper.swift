//
//  GameMapper.swift
//  Gameformation
//
//  Created by Luis Genesius on 22/11/21.
//

import Foundation

/*
final class GameMapper {
    
    static func mapGameResponseToGameModels(input gameResponse: GameResponse) -> [GameModel] {
        return gameResponse.games.map { result in
            return GameModel(
                id: result.id,
                name: result.name,
                released: result.released,
                backgroundImage: result.backgroundImage,
                overallRating: result.overallRating
            )
        }
    }
    
    static func mapGameEntitiesToGameModels(input gameEntities: [GameEntity]) -> [GameModel] {
        return gameEntities.map { result in
            return GameModel(
                id: Int(result.id),
                name: result.name ?? "Unknown",
                released: result.released,
                backgroundImage: result.backgroundImage,
                overallRating: result.overallRating
            )
        }
    }
}
*/
